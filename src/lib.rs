mod vdom;

/// Compiles handlesbars files into virtual dom rendering files.
/// 
/// Usage in `build.rs`:
/// 
/// ```rust
/// fn main() {
/// 
/// }
/// ```
pub fn template_codegen() {
    
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
