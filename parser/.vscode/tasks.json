{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pre-parse-test",
            "command": "rm ../target/debug/handlebars_vdom_parse-*; cargo test --no-run && rm ../target/debug/handlebars_vdom_parse-*.d && cp ../target/debug/handlebars_vdom_parse-* ../target/debug/handlebars_vdom_parse-test",
            "type": "shell",
            "presentation": {
                "reveal": "never"
            }
        }
    ]
}